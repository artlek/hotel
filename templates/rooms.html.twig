{% extends 'base.html.twig' %}

{% block title %}Room list - Hotel App{% endblock %}

{% block body %}

<table>
    <thead>
        <th>no</th>
        <th>type</th>
        <th>price</th>
        <th>availability</th>
        <th>guest name</th>
        <th>guest surname</th>
        <th>guest telephone</th>
    </thead>
    {% for room in rooms %}
    <tbody>
        <td><a href="/room/{{ room.no }}">{{ room.no }}</a></td>
        <td>{{ room.type }}</td>
        <td>{{ room.price }} </td>
        <td>{% if room.availability == 1 %} booked {% else %} free {% endif %}</td>
        <td>{{ room.guestName }}</td>
        <td>{{ room.guestSurname }}</td>
        <td>{{ room.guestTel }}</td>
    </tbody>
    {% endfor %}
</table>

{% endblock %}

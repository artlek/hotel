<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
        <link href="{{ asset('style/style.css') }}" rel="stylesheet"/>
</head>
    <body>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous"></script>
        <div class="container-fluid blue-bar">
            <div class="container-xl blue-bar" data-bs-theme="dark">
                {% block menu %}
                    <nav class="navbar navbar-expand-lg blue-bar">
                        <div class="container-fluid">
                            <a class="navbar-brand" href="">
                                <img src="{{ asset('/uploads/') }}logo.png" alt="Hotel App" width="42" height="42">
                            </a>
                            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                                <span class="navbar-toggler-icon"></span>
                            </button>
                            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                                <div class="navbar-nav">
                                    <a class="nav-link main-menu" href="#">Home</a>
                                    <a class="nav-link main-menu" href="{{ path('rooms') }}">Rooms</a>
                                    <a class="nav-link main-menu" href="{{ path('checkins') }}">Checkins</a>
                                    <a class="nav-link main-menu" href="#">About</a>
                                </div>
                            </div>
                        </div>
                    </nav>
                {% endblock %}
            </div>
        </div>
        <div class="container-xl my-4">
            {% for message in app.flashes('positive') %}<div class="alert alert-success rounded-0">{{ message }}</div>{% endfor %}
            {% for message in app.flashes('negative') %}<div class="alert alert-danger rounded-0">{{ message }}</div>{% endfor %}
            {% for message in app.flashes('neutral') %}<div class="alert alert-secondary rounded-0">{{ message }}</div>{% endfor %}
            {% block body %}{% endblock %}
        </div>
    </body>
</html>

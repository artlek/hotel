{% extends 'base.html.twig' %}

{% block title %} Room {{ room.no }} - Hotel App{% endblock %}

{% block body %}

{% if room is defined %}
    Room number {{ room.no }} <br>
    type {{ room.type }} <br>
    price {{ room.price }} <br>
    availability {% if room.availability == 1 %} booked {% else %} free {% endif %} <br>
    Guest name {{ room.guestName }} <br>
    Guest surname {{ room.guestSurname }} <br>
    Guest telephone {{ room.guestTel }} <br>
{% endif %}

{% if form is defined %}
    {{ form_start(form) }}

        {{ form_label(form.guestName) }}
        {{ form_errors(form.guestName) }}
        {{ form_widget(form.guestName) }}
        {{ form_help(form.guestName) }}

        {{ form_label(form.guestSurname) }}
        {{ form_errors(form.guestSurname) }}
        {{ form_widget(form.guestSurname) }}
        {{ form_help(form.guestSurname) }}

        {{ form_label(form.guestTel) }}
        {{ form_errors(form.guestTel) }}
        {{ form_widget(form.guestTel) }}
        {{ form_help(form.guestTel) }}
        
        <button type="submit">check in</button>
        
    {{ form_end(form) }}
{% endif %}

{% if checkoutForm is defined %}
    {{ form_start(checkoutForm) }}

        {{ form_row(checkoutForm.checkout, { 'label': 'Check out' }) }}

    {{ form_end(checkoutForm) }}
{% endif %}

{% endblock %}
